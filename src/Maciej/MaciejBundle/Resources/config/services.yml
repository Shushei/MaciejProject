services:
    FileUploader:
        class:  Maciej\MaciejBundle\Service\FileUploader
        arguments:
            $args:
                logo :      '%logo_directory%'
                company:    '%company_directory%'
                image:      '%gameimage_directory%'